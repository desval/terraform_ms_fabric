# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
crash.log
crash.*.log

# Secrets — never commit these
*.tfvars.local
override.tf
override.tf.json
*_override.tf
*_override.tf.json
